# MongoDB Configuration
# Get your MongoDB connection string from MongoDB Atlas (https://www.mongodb.com/cloud/atlas)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name

# JWT Token Secret
# Generate a secure random string for production (use: openssl rand -base64 32)
TOKEN_SECRET=your_super_secret_jwt_token_here_change_this_in_production

# Application Domain
# For local development: http://localhost:3000
# For production: https://your-app-name.vercel.app
DOMAIN=http://localhost:3000

# Email Service Configuration (Mailtrap for development)
# Sign up at https://mailtrap.io for development email testing
MAILTRAP_HOST=sandbox.smtp.mailtrap.io
MAILTRAP_PORT=2525
MAILTRAP_USER=your_mailtrap_user
MAILTRAP_PASS=your_mailtrap_password

# Email Configuration
# The "from" email address for sending emails
EMAIL_FROM=noreply@yourdomain.com

# Production Email Service (Optional - for production use)
# Uncomment and configure if using Gmail, SendGrid, AWS SES, etc.
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password
